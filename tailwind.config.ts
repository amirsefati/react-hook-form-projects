import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        myFont: ["vazir", "vazir"], 
      },
    },
  },
  plugins: [],
};

export default config;
